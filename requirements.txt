# --- Core orchestration & LLM ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-ollama>=0.2.0
langchain-anthropic>=0.2.0        # final demo only; harmless to install

# --- Validation ---
pydantic>=2.7.0

# --- Retrieval / RAG (local, free) ---
chromadb>=0.5.0
sentence-transformers>=3.0.0      # local embedding fallback (all-MiniLM-L6-v2)

# --- Data processing ---
pandas>=2.2.0
openpyxl>=3.1.0                   # Excel (.xlsx) reading

# --- Dashboard & charts ---
streamlit>=1.36.0
plotly>=5.22.0

# --- Observability (free tier) ---
langsmith>=0.1.80

# --- Utilities ---
python-dateutil>=2.9.0
